import React from 'react';
import { TouchableOpacity } from 'react-native';
import { button, text } from '../../assets/theme/styles';

export const CardImage = ({datos}) => {
  const onPress = async () => {

  };

  return (
        <TouchableOpacity>

        </TouchableOpacity>
  )

};